# Copy to .env.local for local overrides (gitignored by Vite).
# VITE_* variables are inlined into the client bundle at build time.
# Do NOT put secrets here — use `wrangler secret put` for Worker secrets.

# Google Analytics 4 Measurement ID. Leave empty to disable analytics entirely.
VITE_GA_ID=
